import { SpecificNumberDigitDirective } from './specific-number-digit.directive';

describe('SpecificNumberDigitDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecificNumberDigitDirective();
    expect(directive).toBeTruthy();
  });
});
