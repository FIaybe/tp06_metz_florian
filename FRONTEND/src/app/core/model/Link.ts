export class Link {
  label: string;
  link: string;
  index: number;

  constructor(label: string, link: string, index: number) {
    this.label = label;
    this.link = link;
    this.index = index;
  }
}
